#include "turtlegraphics.hpp"
#include "lsystem.hpp"
#include <iostream>
#include <regex>

int main (int argc, char** argv){
	std::cout << "Hello, compile-time error free world!" << std::endl;
	// Turtle t = Turtle();
	// t.turnLeft();
 //    std::cout << t.getLoc().x << ", " << t.getLoc().y <<" @ " << t.getDir() << std::endl;
	// t.goForward();
	// std::cout << t.getLoc().x << ", " << t.getLoc().y <<" @ " << t.getDir() << std::endl;
	// t.turnRight();
	// std::cout << t.getLoc().x << ", " << t.getLoc().y <<" @ " << t.getDir() << std::endl;
	// t.goForward();
	// std::cout << t.getLoc().x << ", " << t.getLoc().y <<" @ " << t.getDir() << std::endl;
	// t.turnRight();
	// std::cout << t.getLoc().x << ", " << t.getLoc().y <<" @ " << t.getDir() << std::endl;
	// t.goForward(5.0);
	// std::cout << t.getLoc().x << ", " << t.getLoc().y <<" @ " << t.getDir() << std::endl;
	return 0;
}